{
	"name": "obsidian-heading-adjuster",
	"version": "1.3.0",
	"description": "Obsidian plugin to increase or decrease Markdown heading levels across a whole note, a selection, a given line range, or just the current line.",
	"main": "main.js",
	"scripts": {
		"dev": "node esbuild.config.mjs",
		"build": "tsc -noEmit -skipLibCheck && node esbuild.config.mjs production",
		"version": "node version-bump.mjs && git add manifest.json versions.json",
		"test": "node --disable-warning=MODULE_TYPELESS_PACKAGE_JSON --import ./tests/support/resolveTypeScript.mjs --test \"tests/**/*.test.ts\"",
		"lint": "eslint src tests"
	},
	"keywords": [],
	"author": "Valentin Pelletier",
	"license": "MIT",
	"engines": {
		"node": ">=22"
	},
	"devDependencies": {
		"@types/node": "^22.18.0",
		"esbuild": "^0.25.2",
		"eslint": "^9.37.0",
		"globals": "^16.5.0",
		"obsidian": "^1.13.1",
		"tslib": "2.4.0",
		"typescript": "^5.9.2",
		"typescript-eslint": "^8.46.0"
	}
}
